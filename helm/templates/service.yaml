apiVersion: v1
kind: Service
matadata:
  name: {{ .Release.Name}}-servise
  labels:
    app: {{ .Release.Name}}
spec:
  type: {{.Values.service.type}}
  ports:
    - port: {{ .Values.service.port}}
      targetPort: {{.Values.service.targetPort}}
  selector:
    app: {{.Release.Name}}